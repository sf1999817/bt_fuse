通过pid,pathname -> fd

使用tp挂载到sys_enter_openat事件上,当进程打开文件时,会调用openat系统调用,从而触发该事件
  通过触发事件的进程的PCB,可以获取到该进程的文件描述表大小 -- 用于遍历文件描述符表
  通过探测openat,可以获取用户传入的参数(文件路径名) -- 用于后续的比对
  通过proc虚拟文件系统,拿到指定进程的文件描述符信息,这些信息的内容是符号链接,指向打开文件的路径,从而可以获取文件名
  在遍历过程中,与特定路径进行比对,比对成功即可拿到该文件的fd
